object Host "slspledi01.ad.sunlandls.com" {
  check_command = "hostalive" //check is executed on the master
  address = "10.10.0.49"
  vars.os = "Linux" 
  vars.client_endpoint = "slspledi01.ad.sunlandls.com" //follows the convention that host name == endpoint name
  //vars.notification.mail = "slspledi01.ad.sunlandls.com"
}
object Host "SLSTWINFORDB01.ad.sunlandls.com" {
	 check_command = "hostalive" //check is executed on the master
	 address = "10.10.0.33"
	 vars.os = "Windows"
  vars.infor_client_endpoint = "SLSTWINFORDB01.ad.sunlandls.com" //follows the convention that host name == endpoint name
}
object Host "SLSPWSULDB01.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows" 
	address = "10.10.0.16"
  vars.suldb01_client_endpoint = "SLSPWSULDB01.ad.sunlandls.com" //follows the convention that host name == endpoint name
}
object Host "SLSPW3PLAPP02.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.30"
  vars.camelotapp02_client_endpoint = "SLSPW3PLAPP02.ad.sunlandls.com" //follows the convention that host name == endpoint name
}
object Host "SLSPW3PLDB02.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.23"
  vars.camelotdb02_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "SLSPW3PLDB03.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.47"
  vars.camelotdb03_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "SLSPWSCE01.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.6"
  vars.sceapp01_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "SLSPWSCEDB01.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.9"
  vars.scedb01_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "SLSPWSCEDB02.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.8"
  vars.scedb02_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "SLSPWSULDB02.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.17"
  vars.suldb02_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "SLSPWSULAPP01.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.15"
  vars.sulapp01_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "SLSPWShare01.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.20"
  vars.share01_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "SLSPWWAP01.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.16.10"
  vars.pwwap01_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "SLSPWMLE01.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.38"
  vars.pwmle01_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "SLSPWWANFAPP01.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.24"
  vars.pwwanfapp01_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "SLSPWUTILAPPS01.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.35"
  vars.pwutilapp01_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "SLSPWION01.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.10"
  vars.pwion01_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "SLSPWSPS01.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.18"
  vars.pwsps01_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "SLSPWXDB01.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.40"
  vars.pwxdb01_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "SLSPWIFS01.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.11"
  vars.pwifs01_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "REMOTE.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.48"
  vars.remote_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "SLSPWSCEDBW01.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.0.7"
  vars.pwscedbw01_client_endpoint = name //follows the convention that host name == endpoint name
}
object Host "SLSPWSCEWEB01.ad.sunlandls.com" {
        check_command = "hostalive" //check is executed on the master
        vars.os = "Windows"
        address = "10.10.16.5"
  vars.pwsceweb01_client_endpoint = name //follows the convention that host name == endpoint name
}
